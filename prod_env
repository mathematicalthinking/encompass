
NODE_ENV="production" # to set environment to production
PORT=1339 # to set IP port for production
DB_NAME="encompass_prod" # to set the database for production

# to send emails through gmail for production
# note: this overrides the TEST_GMAIL_USERNAME and TEST_GMAIL_PASSWORD
GMAIL_USERNAME="encompassmath@gmail.com"
GMAIL_PASSWORD="stemdev21"

# directory where final version of build files end up
BUILD_DIR='dist'
