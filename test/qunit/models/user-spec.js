/* global emq, moduleFor, moduleForModel, sinon */
// Computed props from selection model should work as expected

/*
QUnit.test("Start again with unit tests", function( assert ){
  assert.equal( true, true);
});

QUnit.test("Start over  with unit tests 2", function( assert ){
  assert.equal( true, true);
});

emq.globalize();
emq.setResolver(Ember.DefaultResolver.create({ namespace: Encompass }));

moduleForModel('user', 'User Model');

test('isAuthenticated, isAuthz', function() {
  var user = this.subject({
    name: 'Damola',
    username: 'aim27',
    isGuest: true,
    isAuthorized: false,
    isAdmin: false
  });

  equal(user.get('isAuthenticated'), false);
  equal(user.get('isAuthz'), false);

  Ember.run(function() { 
    user.set('isGuest', false);
  });

  equal(user.get('isAuthenticated'), true);
  equal(user.get('isAuthz'), false);

  Ember.run(function() { 
    user.set('isAuthorized', true);
  });

  equal(user.get('isAuthenticated'), true);
  equal(user.get('isAuthz'), true);
});

test('displayName', function() {
  var user = this.subject({
    username: 'aim27'
  });

  equal(user.get('displayName'), 'aim27');

  Ember.run(function() { 
    user.set('name', 'Damola');
  });

  equal(user.get('displayName'), 'Damola');
});
*/
