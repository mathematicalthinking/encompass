{{page-title @model.assignment.name}}
<AssignmentInfo
  @sections={{@model.sections}}
  @assignment={{@model.assignment}}
  @groups={{@model.groups}}
  @assignmentStudents={{@model.students}}
  @currentProblem={{@model.currentProblem}}
  @currentSection={{@model.currentSection}}
  @linkedWorkspaces={{@model.linkedWorkspaces}}
  @parentWorkspace={{@model.parentWorkspace}}
  @answers={{@model.answers}}
  @onAnswerCreated={{this.onAnswerCreated}}
  @isStudent={{@model.isStudent}}
/>