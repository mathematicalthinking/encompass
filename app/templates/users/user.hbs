{{page-title this.model.user.username}}
<div id='user-info'>
  <UserInfo
    @user={{this.model.user}}
    @refresh={{this.refresh}}
    @userSections={{this.model.userSections}}
    @orgList={{this.model.organizations}}
  />
</div>