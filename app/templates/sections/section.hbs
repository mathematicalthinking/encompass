{{page-title this.model.section.name}}
<SectionInfo @section={{this.model.section}} @cachedProblems={{this.model.cachedProblems}} @students={{this.model.students}} @currentUser={{this.model.currentUser}} @groups={{this.model.groups}} @refresh={{action "refresh"}} />
