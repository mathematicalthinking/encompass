{{top-bar store=store user=currentUser isHidden=isHidden toHome="toHome"}}
{{socket-loader store=store currentUser=currentUser}}

<main id="al_body">

  {{outlet}}

  {{#if showCategoryList}}
  <div id="category-list-modal">
    <div class="modal-content">
      <h4>Select a Category</h4>
      <button class="close" {{action 'closeModal'}}><i class="fas fa-times"></i></button>
      {{categories-menu categories=categories store=store addCategories="searchCategory"}}
    </div>
  </div>
  {{/if}}

</main>

