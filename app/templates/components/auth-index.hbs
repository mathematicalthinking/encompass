<h1>Main Auth</h1>

{{#if shouldShowLogin}}
  <button {{action 'changeComponent'}}>Sign up</button>
{{else}}
  <button {{action 'changeComponent'}}>Log in</button>
{{/if}}

{{#if shouldShowLogin}}
  {{log-in}}
{{else}}
  {{sign-up}}
{{/if}}
{{outlet}}

{{!-- THIS NEEDS TO RENDER BOTH COMPONENTS AND ONLY DISPLAY ONE AT A TIME
TOGGLE BETWEEN BOTH --}}