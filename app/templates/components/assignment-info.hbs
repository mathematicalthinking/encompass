<header class="info-header">
  <h2>Assignment Details</h2>
  <div class="clear"></div>
</header>
{{#each this.initialLoadErrors as |error|}}
    <p class="error-message">{{error}}</p>
{{/each}}
{{#if this.currentUser.isStudent}}
  <AssignmentInfoStudent @assignment={{this.assignment}} @problem={{this.currentProblem}} @section={{this.currentSection}} @answerList={{this.assignment.answers.content}} />
{{else}}
  <AssignmentInfoTeacher @groups={{this.groups}} @sections={{this.sections}} @assignment={{this.assignment}} @currentUser={{this.currentUser}} @details={{this.details}} @studentList={{this.assignmentStudents}} @store={{this.store}} @section={{this.currentSection}} @problem={{this.currentProblem}} @toAssignments="toAssignments" @linkedWorkspaces={{this.assignment.linkedWorkspaces.content}} @parentWorkspace={{this.assignment.parentWorkspace.content}} />
{{/if}}








