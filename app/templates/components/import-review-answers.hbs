<div class="import-review-answers-page">
  <p class="info">Please review your submissions to ensure everything is accurate. You can either defer creating a workspace until a later date, or we can automatically create a workspace for you from your uploaded submissions</p>

  <div class="workspace-options">
  <h3>Workspace Creation Options</h3>
  <label for="createWorkspace">Automatically create workspace</label>
  {{input type="checkbox" name="createWorkspace" checked=doCreateWorkspace}}

  <label for="workspaceMode">Make workspace private</label>
  {{input type="checkbox" name="workspaceMode" checked=isPrivate}}

  <div class="proceed">
    {{#if doCreateWorkspace}}
      {{#if isPrivate}}
        <p>Click submit to create your private workspace.</p>
    {{else}}
        <p>Click submit to create your public workspace.</p>
      {{/if}}
    {{else}}
      <p>Click submit to upload your submissions.</p>
    {{/if}}

    <button {{action "formatAnswers"}}>Submit</button>
  </div>
</div>

  <div class="review-answers">
  {{#each answers as |answer|}}
    <div class="answer-info">
      <p>Author: {{answer.student.username}}</p>
    </div>

    <p>Submission: {{{answer.explanation}}}</p>
  {{/each}}
</div>


</div>



