<p class="input-label">
  Select Files to Upload
  <span class="info-text-tip simptip-position-right simptip-multiline simptip-smooth" data-tooltip="We currently only accept pictures and pdfs">
    <i class="fas fa-info-circle info-icon"></i>
  </span>
</p>
<p class="sub-input-label">Supported files are jpeg, png or pdf. Multipage pdfs will convert each page into a single image.</p>
{{image-upload class="form-file" filesToBeUploaded=selectedFiles uploadResults=uploadedFiles acceptMultiple=true}}

{{#if missingFiles}}
  {{error-box error="You must upload at least one file to continue" resetError=(action (mut missingFiles) null) showDismiss=true}}
{{/if}}

<div class="nav-btn-container">
  <button class="primary-button cancel-button" {{action "back"}}>Back</button>
  <button class="primary-button" {{action "next"}}>Next</button>
</div>
