<p class="input-label">
  Select a Class
  <span class="info-text-tip simptip-position-right simptip-multiline simptip-smooth" data-tooltip="Start typing or use the drop down to select the name of the problem you would like to use">
    <i class="fas fa-info-circle info-icon"></i>
  </span>
</p>
<p class="sub-input-label">If this work was completed by members of a class, select the existing class you would like to use or you can create one {{#link-to 'sections.new' classNames='new-link'}}here{{/link-to}}.</p>
{{selectize-input
  store=store
  inputId="select-class"
  isAsync=true
  maxItems=1
  onItemAdd=(action "setSelectedSection")
  onItemRemove=(action "setSelectedSection")
  labelField="name"
  searchField="name"
  valueField="id"
  model="section"
  queryParamsKey="query"
  searchCriterion="name"
  topLevelQueryParams="searchBy"
  placeholder="Search for classes by name..."
  preload="focus"
}}

<label for="noClass">
  {{input type="checkbox" name="noClass" checked=isChecked}}
  <span>I don't want to use a class</span>
</label>


{{#if missingSection}}
  {{error-box error="Please select a class or no class" resetError=(action (mut missingSection) null) showDismiss=true}}
{{/if}}

<div class="nav-btn-container">
  <button class="primary-button cancel-button" {{action "back"}}>Back</button>
  <button class="primary-button" {{action "next"}}>Next</button>
</div>
