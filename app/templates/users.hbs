{{#if this.model.currentUser.isStudent}}
  <div class="centered">FORBIDDEN</div>
{{else}}
  <UserList @store={{this.store}} @currentUser={{this.model.currentUser}} @users={{this.model.users}} @organization={{this.model.organiztions}} @toUserInfo="toUserInfo" />
{{/if}}



<div class="list-outlet users">
  {{outlet}}
</div>

