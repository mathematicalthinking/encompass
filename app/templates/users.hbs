{{#if this.currentUser.isStudent}}
  <div class='centered'>FORBIDDEN</div>
{{else}}
  <UserList
    @users={{this.model.users}}
    @organizations={{this.model.organiztions}}
    @trashedUsers={{this.model.trashedUsers}}
  />
{{/if}}

<div class='list-outlet users'>
  {{outlet}}
</div>