{{page-title 'Problems'}}
<div id='problem-list-container'>
  <ProblemListContainer
    @problems={{@model.problems}}
    @organizations={{@model.organizations}}
  />
  {{#if this.isDetailActive}}
    <div class='list-outlet' id='outlet'>
      {{outlet}}
    </div>
  {{/if}}
</div>